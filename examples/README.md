# Examples

| APP | Description |
|-----|-------------|
| `sched_demo` | `task_delay` + `task_yield` multitasking |
| `mutex_demo` | Priority inheritance scenario |
| `stress_test` | Ten tasks, one mutex |

```bash
make APP=sched_demo && make flash
```
